spring:
   application:
      name: CONFIG-DEV          
eureka:
   instance: 
      prefer-ip-address: true
   client:
      fetch-registry: true
      registry-with-eureka: true
      service-url:
        defaultZone: http://localhost:8087/eureka


